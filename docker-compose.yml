services:
  workspace-secretary:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: workspace-secretary
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./config:/app/config
    environment:
      - LOG_LEVEL=INFO
